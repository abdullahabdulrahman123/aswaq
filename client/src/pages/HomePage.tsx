/**
 * الصفحة الرئيسية — فاضية دلوقتي بطلب العميل.
 * الهيرو وأقسام المنتجات والشركات والتصنيفات اتشالوا لحد ما يتحدد
 * شكل الصفحة. الكود بتاعهم في تاريخ جيت لو حبينا نرجّعهم.
 */
export function HomePage() {
  return <div className="mx-auto max-w-6xl px-4 py-8" />;
}
